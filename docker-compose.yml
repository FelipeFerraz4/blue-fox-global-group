services:
  blue-fox-app:
    container_name: blue-fox-global-group
    restart: unless-stopped
    build:
      context: ./
      dockerfile: Dockerfile
    expose:
      - "4000"
    networks:
      - infra-net

networks:
  infra-net:
    external: true